apiVersion: v1
kind: Service
metadata:
  name: multi-region-service
spec:
  type: NodePort
  selector:
    app: multi-region-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30000
